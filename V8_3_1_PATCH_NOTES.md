# v8.3.1 readability + progress fix

What changed:
- set progress now updates gradually from real learning signals instead of only full mastery flags
- stronger readability pass for cards, inputs, tables, helper text, and progress surfaces
- new words-per-session control in the Growth Cockpit
- matching words-per-session control added to Set Details
- set details now shows clearer learning progress and mastery progress

Notes:
- the custom session cap is optional; leave it empty or press Auto to keep adaptive sizing
- progress now reflects learning momentum, while the strong/mastery count still shows how many words are fully stable
